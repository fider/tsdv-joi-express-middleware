{
  "name": "tsdv-joi-express-middleware",
  "version": "1.1.3",
  "description": "Request Validator",
  "main": "./lib/app/index.js",
  "types": "./lib/app/index.d.ts",
  "author": "Lenovo Software",
  "license": "ISC",
  "publishConfig": {
    "registry": "https://usartifactory.lenovonet.lenovo.local/artifactory/api/npm/lenovo-auto-delete-npm-local"
  },
  "scripts": {
    "build": "rm -rf ./lib && tsc -p tsconfig.json",
    "build:publish": "rm -rf ./lib && tsc -p tsconfig.json --sourceMap false",
    "test": "jest",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "tsdv-joi": "fider/old-stable-tsdv-joi"
  },
  "__recommended": "Recommended devDependencies for local-tsdv-joi:    npm i -D @hapi/joi@15.0.1 @types/hapi__joi@15.0.1",
  "devDependencies": {
    "@hapi/joi": "^15.0.1",
    "@types/express": "4.16.1",
    "@types/hapi__joi": "^15.0.1",
    "@types/jest": "^24.0.13",
    "@types/node": "9.4.7",
    "deepcopy": "^2.0.0",
    "express": "^4.17.1",
    "jest": "^24.8.0",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.1"
  }
}
